/**
 * This is a test class that tests vault class that simulates a vault. 
 *
 * @author Patrick Zhou
 * @version v1.0.0
 */

public class VaultTester {
    public static void main(String[] args) {
        // declare and initialize the 
        Vault myObject = new Vault("hacked");
        System.out.println(myObject.getHint("encash"));
        System.out.println(myObject.getHint("encash"));
        System.out.println(myObject.getHint("encash"));
        System.out.println(myObject.getHint("encash"));
        System.out.println(myObject.getHint("encash"));
        System.out.println(myObject.getHint("encash"));
        System.out.println(myObject.getHint("encash"));
    }
}
